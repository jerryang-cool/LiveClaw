# OpenClaw Agent Live - Python Dependencies
# Install: pip install -r requirements.txt

# Core: RTMP streaming (H.264 + AAC encoding via PyAV)
av>=10.0.0

# Core: Frame rendering (Pillow-based dashboard)
Pillow>=9.0.0

# Core: Audio/frame data processing
numpy>=1.21.0

# Optional: Music search skill (HTTP requests)
requests>=2.28.0

# Optional: Windows process management
# psutil>=5.9.0  # Uncomment on Windows
